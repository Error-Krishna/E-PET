# E-Pet Control Center

The native control center now uses the shared project documentation in [../README.md](../README.md) and the shared dependency file in [../requirements.txt](../requirements.txt).

Use:

```bash
python epet_gui/main.py
```
